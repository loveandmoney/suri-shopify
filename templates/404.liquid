
<section class="not-found w-screen md:h-screen relative md:flex md:items-center md:justify-center overflow-hidden mb-32 md:mb-0">
  <!-- <div class="animation-appear-slow animation-delay-5 w-full md:h-full md:absolute md:top-0 md:right-0 md:bottom-0 md:left-0 flex items-center justify-end">
    <figure class="w-full md:h-full relative">
      <img src="{{ '404-desktop.png' | asset_url }}" class="w-full h-full relative hidden md:block object-cover" alt="Three brush colours lined up" />
      <img src="{{ '404-mobile.png' | asset_url }}" class="w-full relative md:hidden" alt="Three brush colours lined up" />
    </figure>
  </div> -->

  <div class="animation-appear-down-slow animation-delay-2 w-full absolute md:relative bottom-0 md:bottom-auto right-0 md:right-auto left-0 md:left-0 z-10">
    <div class="grid">
      <article class="col-span-12 md:col-span-5 md:col-start-2 relative">
        <h1 class="mb-8 h2">Page not found</h1>
        
        <!-- <a href="/">
          <button type="submit" name="checkout" class="button button--green w-full md:w-auto h-12 relative block md:px-32">
            <span class="button-text">
              Go back home
            </span>
          </button>
        </a> -->
      </article>
    </div>
  </div>
</section>